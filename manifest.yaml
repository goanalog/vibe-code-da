apiVersion: v1
kind: deployableArchitecture
metadata:
  name: vibe-ide-da
  version: 1.3.3
  description: Deploys the Vibe IDE with IBM Cloud Object Storage and public access, integrated with Cloudflare broker for user manifests.
  license: Apache-2.0
  labels:
    - vibe
    - ide
    - ibm-cloud
    - cos
spec:
  type: terraform
  provider: ibm
  primaryOutputs:
    - ide_url
    - app_url
  tags:
    - static-site
    - no-input
